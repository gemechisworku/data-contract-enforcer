[project]
name = "data-contract-enforcer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
profiling = ["ydata-profiling>=4.0"]
